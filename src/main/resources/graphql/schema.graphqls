type Query {
  getCharacterById(id: ID!): Character
  getStarshipById(id: ID!): Starship
}

enum Episode {
  A_NEW_HOPE
  THE_EMPIRE_STRIKES_BACK
  RETURN_OF_THE_JEDI
}

type Starship {
  id: ID!
  name: String!
  length: Int  
}

type Character {
  id: ID!
  name: String!
  appearsIn: [Episode]!
}